{
  "name": "mahiron",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c cyan,green \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm run dev --workspace @mahiron/server",
    "dev:web": "npm run dev --workspace @mahiron/web",
    "check": "npm run check --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "verify": "npm run check && npm test",
    "probe": "npm run probe --workspace @mahiron/server --",
    "infra:up": "docker compose up -d",
    "infra:down": "docker compose down",
    "postinstall": "node scripts/prisma-generate.mjs --optional",
    "prisma:generate": "node scripts/prisma-generate.mjs"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "prisma": "^6.19.3"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
