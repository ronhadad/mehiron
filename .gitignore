node_modules/
.next/
.next-build/
dist/
*.tsbuildinfo

.env
.env.local
!.env.example

# Captured Google HTML used to reproduce a parse locally. Fixtures under
# server/test/fixtures are checked in on purpose; ad-hoc captures are not.
scratch/
*.log
.DS_Store

# Prisma writes the dev database URL here.
server/.env

# Local setup notes, contains a generated secret.
VERCEL-SETUP.local.txt
